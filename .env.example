# .env.example for scienceos-2api

# --- 安全与认证 ---
# 用于保护您的API服务的万能主密钥，请替换为您自己的复杂密钥
API_MASTER_KEY=1
# --- ScienceOS 核心认证信息 ---
# 从浏览器开发者工具中获取的 Authorization Bearer Token
SCIENCEOS_AUTH_TOKEN="Bearer eyJhbGciOi..."

# --- 服务端口 ---
# Nginx 对外暴露的端口
LISTEN_PORT=8082

# --- Docker 内部应用端口 (通常无需修改) ---
# Uvicorn 在容器内监听的端口
APP_PORT=8080
